export type styles = {
  root: {
    flexGrow: number;
  };

  grow: {
    flexGrow: number;
  };

  menuButton: {
    marginLeft: number;
    marginRight: number;
  };
};

export type classes = {
  grow: string;
  root: string;
  menuButton: string;
};
