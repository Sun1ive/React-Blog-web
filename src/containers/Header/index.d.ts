export type styles = {
  root: {
    flexGrow: number;
  };

  grow: {
    flexGrow: number;
  };
};

export type Props = {
  classes: classes;
}

export type classes = {
  grow: string;
  root: string;
};
